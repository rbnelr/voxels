using UnityEngine;
using Unity.Mathematics;
using static Unity.Mathematics.math;

public class Flare : MonoBehaviour {
	
	void OnGUI () {
		//GUI.Label(new Rect(0, 40, 500,30), "Flare Vel: "+ GetComponent<Rigidbody>().velocity.magnitude);
	}
}
